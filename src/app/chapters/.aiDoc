---
@META
dir: src/app/chapters
updated: 2026-03-18
files: 3
stack: Next.js 14, React 18, TypeScript

@EXPORTS
layout.tsx: ChaptersLayout — chapters section layout with navigation
page.tsx: ChaptersPage — grid of 40 chapter cards with progress stars & module test triggers
[chapterId]/page.tsx: ChapterDetailPage — full chapter view: video + markdown content + questions + project submission

@PROCESSES
ChapterList: load all chapters from src/data/chapters → fetch user progress from /api/chapters/all-progress → render ChapterCard grid → show ModuleTestModal every 10th chapter
ChapterDetail: fetch chapter by ID → load markdown from /prednasky/ → render video + content + QuestionCard list + ProjectSubmission → complete button → POST /api/progress/complete-chapter

@DEPENDENCIES
internal: @/data/chapters, @/components/chapters/*, @/components/tests/ModuleTestModal, @/components/ui, @/store/user-store
external: next/navigation, react
---
